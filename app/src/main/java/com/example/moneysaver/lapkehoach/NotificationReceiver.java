package com.example.moneysaver.lapkehoach;

import android.app.Notification;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class NotificationReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
//        Notification notification = intent.getParcelableExtra(CommonConstraint.NOTIFICATION_LABEL);
//        int id = intent.getIntExtra(CommonConstraint.NOTIFICATION_ID_LABEL, 0);
//        NotificationUtils.notify(context, notification, id);
    }


}